{
  "name": "Atodo",
  "version": "1.0.7",
  "description": "todo list application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pwwin": "electron-packager ./ ATodo-win --overwrite --platform=win32 --asar --out=Atodo --prune=true --icon=./icon.ico",
    "pwlinux": "electron-packager ./ ATodo-linux --overwrite --platform=linux --asar --out=Atodo --prune=true --icon=./icon.ico"

  },
  "author": "kart jim",
  "license": "MIT",
  "devDependencies": {
    "electron": "^13.1.7"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.3",
    "@popperjs/core": "^2.9.2",
    "bootstrap": "^5.0.2",
    "electron-localstorage": "^1.0.5",
    "jquery": "^3.6.0",
    "yarn": "^1.22.11"
  }
}
